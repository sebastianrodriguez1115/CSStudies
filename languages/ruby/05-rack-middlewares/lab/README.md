# Lab

Mejorar `projects/rack-api` con middlewares.

Debe incluir:

- Logging por request.
- Header `X-Request-Id`.
- Header `X-Response-Time`.
- Auth por bearer token.
- Manejo centralizado de errores.
