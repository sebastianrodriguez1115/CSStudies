# Exercises

- Crear `GET /health` con respuesta `200`.
- Crear `GET /hello?name=...`.
- Crear `POST /echo` que devuelva el body recibido.
- Crear `404` para rutas desconocidas.
- Testear cada endpoint con `rack-test`.
